# Theme screenshots

Run `./scripts/render_theme_screenshots.sh` (requires LibreOffice + pdftoppm or pdf2image) to populate this directory with one PNG per theme. Output: `<theme-name>.png`.

Used for visual reference and README/docs galleries. Re-run after editing themes.
